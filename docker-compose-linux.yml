version: "3.8"

services:
  gazebo:
    build: ./docker
    container_name: gazebo_class
    volumes:
      - ./:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
    privileged: true
